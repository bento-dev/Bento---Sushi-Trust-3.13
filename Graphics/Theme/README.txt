The ubuntu theme text files for the Openbox remix go under /lib/plymouth/themes/ubuntu-text.
Here are the files used and the diff.

The splash.pcx file goes into the isolinux directory in the ISO image.

Les fichiers texte du thème Ubuntu pour le remix Openbox vont sous /lib/plymouth/themes/ubuntu-text.
Les présent fichiers sont ceux utilisés, et leur diff.

Le fichier splash.pcx va sous le répertoire isolinux dans l'image ISO.

